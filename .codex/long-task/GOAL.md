# Goal

## Outcome

Deliver and publicly release `recordly-codex` v0.5.0 as a reproducible Codex Desktop Browser plugin that turns an approved website URL and bounded objective into a professionally composed, evidence-backed recording without another visible recording app. Codex directs and judges the workflow while local deterministic code owns capture, media ingestion, composition, encoding, publication, and artifact verification.

## Constraints

- Keep the repository public-safe from its first commit: no secrets, cookies, captured frames, personal data, private URLs, or unlicensed third-party code/assets.
- Support Codex Desktop Browser first. Do not promise browser control in Codex CLI or IDE environments.
- Permit unattended execution only for public or explicitly pre-authenticated, user-authorized flows. Stop at credentials, CAPTCHAs, browser permissions, payments, sensitive uploads, irreversible actions, and other user-only confirmations.
- Treat Recordly as an upstream reference, not a copy source. Do not incorporate AGPL-3.0 implementation or assets without a written license-compliance decision.
- Use strict TypeScript, red-green-refactor for executable behavior, deterministic manifests, and evidence-backed quality gates.
- Prefer a small vertical slice over a speculative editor clone. No native desktop app or hosted service is in the initial scope.

## Evidence

- `.codex-plugin/plugin.json` and `skills/recordly-codex/SKILL.md` define the Codex-facing contract.
- `README.md`, `docs/architecture.md`, `docs/threat-model.md`, and `docs/licensing.md` define public scope and safety constraints.
- `package.json`, `tsconfig.json`, `vitest.config.ts`, `biome.json`, and `.github/workflows/` define the reproducible engineering gate.
- The public repository is `https://github.com/daniel-p-green/recordly-codex`, with protected `main`. Release commit `db660aa` is tagged `v0.1.0`; PR #8, `main`, and the tag CI are green, and the release source asset is published.
- PR #9 merged to protected `main` at `b1a2f6e8c2de13439fd9f3a3f66fe8070c6c3871`. The resulting `main` and `v0.2.0` tag CI are green, and the validated release workflow published [v0.2.0](https://github.com/daniel-p-green/recordly-codex/releases/tag/v0.2.0). Its `recordly-codex-v0.2.0.tar.gz` source asset is 351,224 bytes with release SHA-256 `74c0d8490a196bed64d96a82e8a4f69fe36eabff4125dd3bd23a706afdf0126a`.
- The completed path includes five stdio MCP tools, a per-session Browser helper and loopback capture broker, immutable frame-hash verification, broker-owned receipt timing, deterministic CFR render/encode, and sealed delivery artifacts.
- The v0.1.0 Git marketplace installation is enabled and pinned to the tag. Its standalone bundle hash is `f6b1d381c0c06cfad8fcfae8775e31a4584b62976f99deec0a6c4270459f8744`; the installed package has no `node_modules`, `dist`, `coverage`, or recording-artifact directories. Raw JSON-RPC verified initialization, exactly five tools, and clean create/discard behavior from that installed package.
- The Git marketplace is pinned to v0.2.0, and the plugin is installed and enabled at version 0.2.0. The installed bundle SHA-256 is `ea7da03dfd3f4ac31d6dfb76a788aac3e2ad5d3d8c6c47e3265e3ceb9f39b0f2`, matching the verified release bundle. Before raw smoke, the installed cache contained no `node_modules`, `dist`, `coverage`, `artifacts`, or `.playwright-mcp` directories; the v0.1.0 cache was absent. Raw JSON-RPC initialization listed exactly the five expected tools, create/discard passed, and stderr was empty. Empty helper directories created by smoke were removed afterward.
- Final approved live evidence exercised the supported authorized `recordly.dev` workflow: 661 source frames produced 669 CFR output frames over 22.3 seconds, with a 1920×1080, 30 fps, H.264 `yuv420p` limited-range, silent MP4. The quality report approved broker-receipt offsets, source/hash privacy, non-frozen samples, and a present final state. Captured pixels, paths, identifiers, and hashes are intentionally not published.
- The v0.2.0 candidate automatically observes bounded trusted-click and trusted-wheel-derived scroll records only after the first durable frame, same-origin document validation, and current document-epoch readiness. Locally, CDP must supply nonempty frame ID, loader ID, URL, and `securityOrigin` exactly matching the recording origin before challenge or isolated-world work. Independently and before marker side effects, the authenticated broker parses `documentUrl`, allows only credential-free HTTP(S), and requires its canonical origin to match session and request origins. Each valid epoch receives a fresh nonpersistent 256-bit marker; at most 32 challenges may be issued. The isolated observer sends `console.debug(marker, envelope)` across the trusted CDP/DevTools boundary; Runtime binding APIs are absent. Readiness requires successful isolated evaluation plus an exact ready handshake that pins the first valid closure-only 256-bit nonce. Exact filtering requires `debug`, two direct-string arguments, the single current marker, payload at most 4 KiB, and exact ready or event shape. Unmatched console traffic, retired marker/nonce pairs, and conflicting later ready nonces are ignored; malformed routed payloads fail closed, and `executionContextId` is diagnostic/non-gating. A→B→A receives fresh epochs, so A's old credentials cannot return. The helper strips marker and nonce before broker event delivery, and the broker never persists challenges, so neither enters evidence or delivery artifacts. Active and in-flight document duplicates are idempotent; a changed loader creates one replacement epoch. Start waits up to 10 seconds for durable baseline plus observer readiness; timeout fails capture and runs per-step 250 ms best-effort teardown through CDP detach and broker failure reporting. Synthetic clicks, generic or programmatic scrolling, invalid document origin/URL, missing document identity after arming, and navigation-gap routed activity are not observed evidence. The broker assigns accepted events its frame clock and persists them privately; planned MCP events remain separate and cannot satisfy approval.
- `test/unit/browser-helper-observed.test.ts`, `test/unit/capture-broker.test.ts`, and `test/integration/sealed-session-render.test.ts` verify local CDP `securityOrigin` rejection before challenge/install, broker HTTP(S)/credential/canonical-origin validation before marker side effects, pre-baseline challenge rejection, authenticated nonpersistent fresh markers and the 32-challenge cap, successful-evaluate plus ready-handshake commitment, first-nonce pinning, exact console filtering with no binding APIs, retired marker/nonce and A→B→A rejection, challenge/artifact exclusion, malformed-routed-envelope failure, non-gating execution-context diagnostics, document-identity idempotence for active and in-flight signals, changed-loader re-observation, the 10-second readiness timeout cannot hang on teardown, trusted-input filtering, generation-serialized navigation activation with fail-closed readiness gaps, stop-time lifecycle and evidence draining, explicit listener cleanup and detach, strict origin/auth/schema/rate/count/persistence boundaries, action receipt alignment, ancestor/realpath containment, decoded visible-result/final-hold checks, and decoded edge/border/matte/aspect clipping checks. Approval now fails closed unless receipt timing, non-frozen evidence, temporal visible-result alignment, clipping, media, hash, final-state, and privacy gates pass. The alignment proof is temporal and explicitly non-causal.
- Final headed v0.2.0 release-candidate acceptance exercised the authorized Recordly workflow through the supported Codex Desktop Browser path. Start returned `observedReady: true`; one trusted-wheel-derived scroll was captured and sanitized. Stop reported 1,010 frames received, accepted, and acknowledged, with zero rejected frames and no degradation.
- The sealed candidate was approved as a silent H.264 `yuv420p` limited-range MP4 at 1920×1080 and 30 fps. Independent probing and full decode verified 1,022 decoded frames over 34.066016 seconds with no audio. Temporal QA aligned the observed action at frame 305 to a visible result at frame 308; visible delta was 0.216615 against the 0.02 threshold, and the final hold was 23.766666 seconds. Clipping, aspect, matte, border, frozen-frame, final-state, privacy, and artifact-hash checks passed. Inspected sensitive evidence files used mode `0600`.
- An independent Terra High audit judged the headed deliverable professionally usable with no release blocker. The latest security review cleared P0–P2. The deterministic v0.2.0 bundle is 944,608 bytes with SHA-256 `ea7da03dfd3f4ac31d6dfb76a788aac3e2ad5d3d8c6c47e3265e3ceb9f39b0f2`. The canonical suite passes 97 tests across 17 files, plus fixture validation, typecheck, bundle reproducibility, and plugin validation.
- The v0.3.0 release expands the public MCP surface to ten tools: the five proven capture-session tools plus project create, inspect, revise, preview, and final. Canonical projects support monotonic revisions and stale-preview rejection; the deterministic renderer supports MP4/GIF, trims, speed ramps, cuts/crossfades, source-keyed cursor/click evidence, manual/automatic zoom, annotations, captions, PiP, WAV audio, and declared hooks. Private exclusive staging prevents hash-then-path-reopen races for capture frames, PiP, audio, and render publication.
- v0.3.0 is publicly released and installed. Commit `5e526b71d279eceb65ea24ce9dff6e9e8ecabfc5` is on protected `main`; CI run `30433535484` passed after a Node 22 infrastructure retry, and [v0.3.0](https://github.com/daniel-p-green/recordly-codex/releases/tag/v0.3.0) is published. The installed enabled server reported 0.3.0, exposed exactly ten tools, passed create/discard smoke with empty stderr, and its bundle SHA-256 matched source at `6ab1455733de81bd8ac259b815f2d428c490335eed4e29ad1bcba5eea2e2a228`.
- Final release acceptance exercised the approved Recordly.dev hero-to-features workflow through Codex Desktop Browser. Capture accepted and acknowledged 892 frames with zero rejected frames. An approved v0.2.0 sealed delivery migrated into a v0.3.0 project; missing cursor evidence remained hidden, a legacy `0644` capture-event file was not used as trusted render evidence, and proportional screencast frames were safely scaled to sealed geometry. The decoded MP4 preview was 1920×1080 at 30 fps for 29.533333 seconds. Revision advanced from 0 to 1 without recapture; the second preview and final shared the same deterministic SHA. Private evidence was mode `0600`, and visual QA passed.

## Milestones

1. Establish public repository governance, plugin scaffold, strict tooling, CI, and tag-gated source release. **Complete:** protected `main`, green PR/main/tag CI, and the v0.1.0 published release asset are verified.
2. Define versioned recording-manifest, shot-plan, and quality-report contracts with red tests. **Complete for the v0.3.0 project contract:** capture manifests, quality reports, and canonical editable projects with monotonic revisions are implemented and tested. A graphical editor is not included.
3. Build a local deterministic vertical slice for a sanitized fixture: fixed viewport, bounded scripted interactions, observed action evidence, and MP4 export. **Complete for v0.3.0:** MP4/GIF project rendering, cursor/click/zoom, overlays, PiP/audio, ramps, transitions, hooks, and one authorized capture-to-project live acceptance are verified.
4. Add visual and technical quality gates with one bounded retry path and a manifest-backed final report. **Complete for the v0.2.0 scope:** deterministic probe, decode, hash/privacy, frozen-frame, receipt-timing, temporal visible-result/final-hold, decoded clipping, final-state, and fail-closed approval gates are implemented and tested. Causal or semantic result proof is not claimed.
5. Validate installability in Codex Desktop, run a real approved-site demonstration, and publish a candid capability/limitation matrix. **Complete for v0.2.0:** protected-main merge, green main/tag CI, published release asset, pinned Git marketplace installation, installed stdio MCP smoke, headed authorized-site acceptance, professional-usability audit, security review, canonical package checks, and the candid capability/limitation matrix are verified.
6. Release and validate v0.3.0. **Complete:** protected-main merge, green CI after infrastructure retry, public release, enabled installation, installed version and exact ten-tool smoke, matching bundle SHA, and authorized Codex Desktop Browser workflow are verified.
7. Release and validate v0.5.0 near-output-parity workflow. **In progress:** V2 project/media/profile contracts, private visual and audio pipelines, professional mixing, preview judgment, decoded visual-media service rendering, capture budgets, tracked-file CI, and the clean-room output-parity fixture contract are implemented in the working tree. Executable parity, remaining MCP/editorial automation, independent release audit, live Browser acceptance, public tag/release, and clean-installed runtime proof remain open.

## Done when

- [x] A public repository contains a valid Codex plugin, contributor/security governance, and reproducible CI/release controls.
- [x] The plugin directs Codex through authorized Browser-only recording workflows without claiming unsupported autonomy.
- [x] A tested local vertical slice produces a 1080p MP4, sanitized delivery manifest, and quality report for the supported approved workflow.
- [x] Automated checks verify broker-timed trusted-click and trusted-wheel-derived scroll actions, temporal decoded visible-result alignment, final hold, output dimensions/duration/decodeability, frozen-frame detection, decoded edge/border/matte/aspect clipping, and declared final state. The visible-result proof is explicitly temporal and non-causal.
- [x] The public documentation names the supported environment, privacy boundary, upstream license boundary, and human-intervention triggers.
- [x] Remaining unsupported workflows and production risks are explicit.
- [ ] Every v0.5.0 output-parity fixture is actually rendered and decoded across landscape, square, and vertical MP4/GIF outputs; declarative manifest validation alone does not satisfy this gate.
- [ ] Imported visual and normalized audio media, recording profiles, preview judgment, bounded revision, and presentation controls are reachable through the shipped MCP/skill workflow and verified from the generated bundle.
- [ ] The full tracked-source CI, security, compatibility, performance, and decoded professional-output gates pass from a clean release candidate.
- [ ] v0.5.0 is merged to protected `main`, tagged and publicly released, clean-installed from the marketplace, raw-stdio smoked at the exact shipped tool/version surface, and accepted on one authorized live Codex Desktop Browser workflow.
