/**
 * The runtime API is deliberately empty until a tested recording contract exists.
 * Capture, rendering, and browser control belong in dedicated modules, introduced
 * through red-green-refactor changes.
 */
export {};
