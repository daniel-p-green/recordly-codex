interface:
  display_name: "Recordly Codex"
  short_description: "Capture and edit approved browser recordings"
  default_prompt: "Use $recordly-codex to capture this approved website flow with broker-timed evidence, then create a versioned editable project and render a quality-checked preview."
policy:
  allow_implicit_invocation: true
