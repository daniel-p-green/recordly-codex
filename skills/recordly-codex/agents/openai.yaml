interface:
  display_name: "Recordly Codex"
  short_description: "Create approved browser recordings with evidence gates"
  default_prompt: "Use $recordly-codex to record this approved website flow from URL to quality-checked delivery."
policy:
  allow_implicit_invocation: true
